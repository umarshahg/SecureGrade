apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: portal-prod
spec:
  selector:
    app: admin-service
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
  type: ClusterIP
